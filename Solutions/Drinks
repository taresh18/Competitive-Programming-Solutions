#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n,i;
    double sum=0.0;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++){
        cin>>a[i];
    }
    for(i=0;i<n;i++){
        sum+=a[i];
    }
    sum=sum/n;
    cout<<sum;
    return 0;
}